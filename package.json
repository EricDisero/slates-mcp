{
  "name": "slates-mcp-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/mcp && npm run build -w packages/cli",
    "typecheck": "node packages/shared/scripts/embed-skills.mjs && tsc --noEmit",
    "clean": "rimraf packages/*/dist",
    "publish:all": "npm run build && npm publish -w packages/shared --access public && npm publish -w packages/mcp --access public && npm publish -w packages/cli --access public"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.7.0"
  }
}
