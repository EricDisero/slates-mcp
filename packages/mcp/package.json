{
  "name": "@slatesvideo/mcp-server",
  "version": "0.2.0",
  "description": "MCP server for Slates — drive AI Video Creation Studio from Claude, Cursor, or any MCP client.",
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "slates-mcp-server": "./dist/server.js"
  },
  "main": "./dist/server.js",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@slatesvideo/shared": "0.2.0",
    "zod": "^3.23.0",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0"
  }
}
